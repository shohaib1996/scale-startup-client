apiVersion: v1
kind: Service
metadata:
  name: scalestartup
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: scalestartup
  type: NodePort
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
